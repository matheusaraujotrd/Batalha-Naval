- unfinished_business: last coordinate hit
- unfinished_business_direction: adjacent direction

A2 -> A3
A2 -> direction?

if hit[0] == next_hit[0] + 1 and hit[1] == next_hit[1]:
    return 0 # down

A2 -> A3 -> A4
X -> 